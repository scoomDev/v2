import { FilterByIsPublishedPipe } from './filter-by-is-published.pipe';

describe('FilterByIsPublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIsPublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
